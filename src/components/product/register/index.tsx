import { Layout } from "components/layout";
import React from "react";

export const RegisterProducts: React.FC = () => {
    return(
        <Layout title="Register Products">
            Here Product content
        </Layout>
    )
}

